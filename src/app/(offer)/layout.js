export default function Layout({ children }) {
  return (
    <div
      style={{
        backgroundColor: "skyblue",
      }}
    >
      10% off on this independence day
      {children}
    </div>
  );
}
